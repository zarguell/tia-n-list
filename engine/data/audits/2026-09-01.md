# Tia N. List audit — 2026-09-01

Verdict: **GOOD**

## Deterministic checks

| Check | Result | Detail |
|-------|--------|--------|
| digest_published | ✓ | exists, 8452B |
| hourly_fresh | ✓ | newest event 2026-09-01T10:57:40 (2h old) |
| last_commit | ✓ | 2026-09-01T12:05 (0h old) |
| analysis_queue | ✓ | 2 pending |
| cti_queue | ✓ | 1 uncovered case |
| ioc_candidates | ✓ | 206 new candidates |
| store_invariants | ✓ | 0 bad event refs, 0 cti story_id mismatches |
| sigma_valid | ✓ | 0 failing rules |
| yara_valid | ✓ | 0 failing rules |
| automation_runs | ✓ | all three scripts ran today |
| live_site | ✓ | 6/6 pages 200 |
| snapshot_pin | ✓ | 2026-09-01 sha256 match |
| coverage | ✓ | 254 records, 118 techniques, 35 sigma, 5 yara |
| fragmentation | ✓ | no duplicate-story fragments |
| dedup_invariants | ✓ | clean (400 eventless ghosts, informational) |
| triage_drift | ✓ | keep/drop 61/26 (70% keep) |
| analysis_queue_age | ✓ | 6 queued |
| language_scan | ✓ | no non-Latin script bleed |

## Log file status

| Automation | Status | Detail |
|------------|--------|--------|
| hourly | ✓ completed | 2026-09-01.log — DONE at 12:05Z. 14 new events, 6 analyses written. Note: `latest.log` symlink points to stale 2026-08-23.log (cronman symlink rot, not a runtime failure). |
| digest | ✓ completed | 2026-09-01.log — DONE at 10:35Z. 11 stories, 4 overrides, NOTIFY sent. |
| cti | ✓ completed | 2026-09-01.log — DONE at 11:38Z. 14 new CTI records, 1 new IOC curated, no sigma/yara written. |

## Quality sampling

**Today's digest (2026-09-01):** Strong. 11 stories, ~8KB prose narrative. Lead is correctly the PaperCut KEV escalation arc (three interlinked stories: emergency patch → KEV addition → Metasploit module). Exchange EOL + PoC convergence is well-argued. Fire Ant section is the best technical piece — covers GRE tunnels, syslog suppression, TACACS compromise, and PCAP exfiltration with specific details. The SLEEPWALKER analysis is excellent — five covert channels, passive trigger model, ESET side-loading. The watchlist at the end is actionable and specific. No AI slop, no em dashes. Every story links resolve. Reads like a human-written threat-intel brief.

**Newest analysis — BraZetsu malware:** Well-written, specific to Group-IB attribution and five version iterations. Grounded in marketplace infrastructure. No filler. Good.

**Newest analysis — RevStealer (fake Claude Opus 5):** Specific (101MB archive, AES-256-CBC payload, debugger timing check). Covers the full kill chain from lure to credential theft. Grounded and actionable.

**Oldest hot analysis — Trezor/ShipMonk breach:** Still accurate. Covers the third-party logistics angle well — phishing risk from exposed order data. The downstream fraud framing is correct. No staleness issues despite being older.

**CTI records:** Chinese Fire Ant record — clean schema, ATT&CK codes T1090/T1572/T1071/T1562/T1543 all plausible for the described techniques (proxy, protocol tunneling, impair defenses, create system process). References link to reporting outlets. PaperCut record — CVE-2026-81578 and CVE-2026-82078 correctly mapped to T1190 (Exploit Public-Facing Application) and T1505 (Server Software Component). Detections flag shows sigma=true which matches the sigma_valid check passing.

**Live site:** Matrix renders cleanly with hot stories ordered correctly. Digest page at `/daily/2026-09-01/` renders the full prose. CTI page shows ATT&CK matrix with correct detection gap/coverage colors. Story links resolve. Page loads fast. One minor issue: the Shai-Hulud Trinitite story appears in the digest JSON and prose but is missing from the "Stories covered" list at the bottom of the digest page (10 shown, 11 in data). This is a rendering-level inconsistency, not a content issue.

## Judgment-layer review

- **Duplicate suspects adjudicated**: 
  - `asec-july-2026-apt-attack-report-south-korea` vs `asec-june-2026-apt-attack-report-south-korea` (Jaccard 1.0): **Dismissed.** These are monthly ASEC reports — June and July editions. Same report series, different time periods. Not duplicates.
  - `gitea-security-advisory-av26-845` vs `over-8-300-gitea-servers-vulnerable-to-code-execution-attacks` (Jaccard 0.5): **Dismissed.** Two distinct stories: one is the CISA KEV addition (advisory), the other is Shadowserver exposure stats. Different angles, different sources. Not merging.
  
- **Digest overrides reviewed**: All overrides well-grounded. Three promotes — PaperCut exploitation arc ( honeypot confirmation + KEV + Metasploit), Exchange convergence (PoC + EOL) — editorially justified. The WordPress AI tools demote is correct (defensive announcement, not operational). Wildcard for Aug 28 go-loader (SNOWLIGHT/VShell RAT) and Aug 26 PhaaS (Balonx) are grounded in content novelty. No recurring patterns needing codification into score.py — the overrides are situational editorial decisions, not systematic scoring gaps.

- **Trends**: Events/day vs new-active-stories/day shows healthy ratios (70-75% story-to-event ratio on busy days, ~90% on slow days). Aug 29-30 dip is a weekend effect. Aug 31 rebound to 87 events / 58 new stories is normal Monday catch-up. No fragmentation trending.

## Fixed this run

None. All automations completed successfully. No broken detection rules. No queue backlog requiring re-runs. No code bugs found.

## Issues found

1. **Stale `latest.log` symlink in tia-hourly**: `/home/ubuntu/repos/cronman/logs/tia-hourly/latest.log` points to `2026-08-23.log` (9 days old). The hourly cron is running fine (today's log exists as `2026-09-01.log`), but the symlink wasn't updated. This is a cronman bug in the log rotation script — the symlink should point to today's log after each run. Low severity since the dated logs are intact, but it breaks any tooling that reads `latest.log`.

2. **Shai-Hulud missing from digest page "Stories covered" list**: The story appears in the digest JSON (`stories` array) and in the prose narrative, but is absent from the rendered "Stories covered" list on `/daily/2026-09-01/`. This is an SSG rendering inconsistency — the page shows 10 stories but the data has 11. Low severity.

## Recommendations

1. **Fix tia-hourly log rotation**: The script that writes to `logs/tia-hourly/` should update the `latest.log` symlink after writing each day's log. This is a cronman fix — file in the cronman repo.

2. **Investigate Shai-Hulud digest page omission**: Check if the SSG `daily` template has a rendering bug that drops the last story from the list, or if the story's slug is being filtered out by a display condition. Minor but worth a look since it's a newly reported story.

3. **No score.py changes needed**: The digest overrides are all situational editorial decisions. No systematic scoring gap was identified.

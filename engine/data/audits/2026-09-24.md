# Tia N. List audit — 2026-09-24

Verdict: **ISSUES**

Driver: `ingest_noise` FAILED (judgment-layer invariant — a source is flooding triage, gamefan 2026-09-16 incident class). All 18 deterministic checks otherwise passed.

## Deterministic checks

| Check | Result | Detail |
|-------|--------|--------|
| digest_published | ✓ | exists, 10681B |
| hourly_fresh | ✓ | newest event 2026-09-24T12:01:39 (1h old) |
| last_commit | ✓ | 2026-09-24T12:23 (0h old) |
| analysis_queue | ✓ | 2 pending (pre-fix count — see Fixed; real value 15) |
| cti_queue | ✓ | 1 uncovered cases (pre-fix count — see Fixed; real value 49) |
| ioc_candidates | ✓ | 507 new candidates |
| store_invariants | ✓ | 0 bad event refs, 0 cti story_id mismatches |
| sigma_valid | ✓ | 0 failing rules (97 rules) |
| yara_valid | ✓ | 0 failing rules (15 rules) |
| automation_runs | ✓* | hourly/digest/cti/safety_net all "no-api-key" — vacuous pass, see Issues |
| live_site | ✓ | 6/6 pages 200 (/, stories, cti, iocs, snapshot, latest digest) |
| snapshot_pin | ✓ | 2026-09-24 sha256 match |
| coverage | ✓ | 538 records, 135 techniques, 97 sigma, 15 yara |
| fragmentation | ✓ | no duplicate-story fragments |
| dedup_invariants | ✓ | clean (4509 eventless ghosts, informational) |
| triage_drift | ✓ | keep/drop 249/383 (39% keep) |
| analysis_queue_age | ✓ | 15 queued, worst ≤48h |
| language_scan | ✓ | no non-Latin script bleed |
| ingest_noise | ✗ | junk source euvd.enisa.europa.eu: 92% dropped (250/270 decisions, 2d); flood entrant euvd.enisa.europa.eu: 271 events/7d = 100% of lifetime; flood entrant radar.offseq.com: 52/7d = 58% of lifetime; flood entrant dev.to: 86/7d = 52% of lifetime |

## Log file status

| Automation | Status | Detail |
|------------|--------|--------|
| hourly | ✓ completed | latest.log fresh: START 13:00:05Z, prior DONE 12:15:31Z (within 2h), 15 DONEs today. Not broken. |
| digest | ✓ completed | DONE 10:50:34Z after START 10:30:04Z. "SKIP: already running" at 10:50:02 was the hourly recover sweep hitting the held lock — benign. Caveat: an earlier START at 01:28:50Z never reached DONE (aborted run). |
| cti | ✓ completed | DONE 11:51:35Z after START 11:30:04Z. Caveats: starts at 01:28:40Z and 01:32:47Z both reached PHASE1 and then died without DONE; my licensed backlog re-run at 12:58:55Z self-SKIPped ("CTI records already updated today" daily guard). |

## Quality sampling

1. **Today's digest** (`digests/2026-09-24.md`): strongest artifact of the run. Rendered page has emoji headline + exec summary + prose, lead is the ShinyHunters/FBI breach with sourced specifics (5,000-entry sample, 404 Media, 2-3TB extortion deadline, PeopleSoft root), and every entry carries CVEs/advisory codes/IOCs (CN=vpn,OU=users cert subjects, AV26-952, watchTowr/Kudelski). Zero em dashes, zero non-Latin bleed, its 17 story links exactly equal `samples.digest_linked_stories` (verified as sets), spot-checked 3/3 live. Ends with a concrete watchlist. Reads like a human-trusted threat-intel report.
2. **Newest analyses** (IBM Guardium CVE-2026-84239, TeamCity CVE-2026-63077): grounded in the event, specific about affected versions/attack path, honest about single-source limits, useful "what to watch". Publication-quality.
3. **Oldest analyses** (Keycloak CVE-2026-95503, IBM MQ HPE NonStop CVE-2026-11716, SolarWinds ARM CVE-2026-28326): stale and sloppy — boilerplate template ("covers recent security developments… This matters because it affects systems users rely on"), a mid-sentence cutoff ("runs without SPN"), garbled duplication ("CVSS 8.8. h" twice, truncated "…(ARM) unau."), and a source line that says "via Mastodon" over a deafnews.it URL. All three are single-source bot CVE notices; this is the weakest content class on the site.
4. **CTI records** (newest `updated_at` 2026-09-24T11:42:24Z: task-stomp, parallels-desktop): schema sane; all ATT&CK IDs (T1059/T1053/T1547/T1099/T1005/T1113/T1068) resolve in `attack/techniques.json` with matching names/tactics; references 5/6 resolve — valtersit.com returns 403 to non-browser clients even with a browser UA (bot wall).
5. **Live site**: matrix renders (22 story links on front page), IOC feeds page fine, digest page has headline+summary+prose, case page content fine — but the case page `<title>` ends mid-sentence: "…The group claim — Tia N. List" (store title truncated at 140 chars).

## Judgment-layer review

- **Duplicate suspects adjudicated** (10 pairs, 0/2 merges used): all dismissed as title-token false positives.
  - Issabel PBX / IBM MQ / Frappe LMS / ArubaOS-CX / Utania OS / Forgejo RCE cluster (jaccard 0.6-0.75): different products, different CVEs — pure "Remote Code Execution Vulnerabilities" token overlap.
  - AhnLab dark-web trio: separate weekly/monthly reports for different periods (Week 2 Sep vs August vs July 2026) — distinct publications.
  - CVE-2026-80981 vs CVE-2026-80982 net/smc UAFs: adjacent CVEs but different functions (`smc_llc_srv_add_link` vs `smc_rx_pipe_buf_release`), separately disclosed — genuinely two stories. Side note: the 80981 story also carries a CVE-2026-80979 event; same-subsystem folding, worth watching but not a merge.
  - No merges performed; `dedup_invariants` passed so no `repair_dedupe` run.
- **Digest overrides reviewed**: recent wildcards are all grounded in content — n-able N-able (KEV-listed pre-auth RCE, 218 unpatched instances), ClickFix TTP, EU CRA 24-hour clock, EtherHiding, Doppelcart, HiveMQ/Element C2, ASCII-smuggling crossover, Singularity eBPF evasion, TukTuk C2, FBI 153M licenses. Each cites specific evidence; no ungrounded picks found. Recurring patterns to codify into `score.py`:
  1. **Repeat-coverage demotes** ("already covered yesterday, no new delta") appear in virtually every digest, ~10-20 entries each — the daily manual tax. `score.py` has a 36h recency half-life but no already-covered penalty.
  2. **Non-CVE TTP/regulatory uplift**: recurring wildcard rationale is "proxy score underranks TTP-level coverage with no CVE" — the severity axis dominates via CVSS.
  3. KEV (+0.5) and EPSS bonuses already exist in `score.py` — that pattern is codified.
- **Trends**: events/day 508→630 (Sep 18→23) with new-active-stories/day 291→368; ratio stable at 0.49-0.61 all week, no drift. Sep 24 partial (284 events / 56 stories by 12:32 UTC). Volume is up but **fragmentation is not trending** — story growth tracks event growth. Ghost stories: 4509 (informational).

## Fixed this run

1. **audit.py queue-count bug → PR #123** (`fix/audit-2026-09-24`): `qlen()` was called with key `"events"` (nonexistent) for `needs-analysis.json` and no key for `cti-queue.json`, so both fell back to counting top-level dict keys — reporting "2 pending" (real 15) and "1 uncovered" (real 49). The wrong cti count masked the >20-backlog condition of this audit's own re-run license. Both files are written as `{"stories": [...]}` and `audit_checks.queue_age` already reads `"stories"`. Fixed, `engine/test_audit.py` ALL PASS, committed on `fix/audit-2026-09-24`, PR opened. Detail-only; pass/fail criteria unchanged.
2. **Site rebuild** (stale-site license — `git status` had uncommitted data changes): ran `engine/ssg.py` to completion; rebuild produced **no site-output diff** (site already current from the 11:51 CTI publish). Idempotent, nothing to commit. The remaining dirty files are other automations' in-flight ingest state (masto/x watermarks, pending desk events) — left alone, not staged.
3. **CTI re-run attempted** (backlog license — cti-queue had 49 > 20 pending and CTI ran today): the documented `run-cti-pass.sh` no longer exists (cron.d cutover to `bin/cronman run`), invoked `bin/cronman run cti-pass` instead — it self-SKIPped on the "CTI records already updated today" guard. Backlog drains at tomorrow's scheduled run unless the guard gets an override.

No merges (0/2), no sigma/yara quarantines (0 failing), no digest re-run (DONE today), no cron repair (hourly fresh).

## Issues found

1. **`ingest_noise` FAIL** — `euvd.enisa.europa.eu` (euvd_bot): 271 events in 7d = 100% of its lifetime, 93% dropped by triage: a textbook junk flood. Also `radar.offseq.com` (52/7d = 58% of lifetime) and `dev.to` (86/7d = 52% of lifetime, 80% dropped — non-security posts like the Bevy animation article). Author handles flooding: `euvd_bot` (270 decisions/93% dropped), `ivycyber@privacysafe.social` (100% dropped), `security_crawler_carl@infosec.exchange` (83%), `hackaday@www.urbanmind.net` (79%), `bobe_bot@mastobot.ping.moi` (75%).
2. **Overnight automation starts abort silently** — digest START 01:28:50Z and cti START 01:28:40Z + 01:32:47Z all reached PHASE1 and died without DONE/FAIL (likely the automations service, which the audit can't confirm — see issue 5). Scheduled cron runs recovered both the same day, so no output was missed, but ~01:28 UTC runs are 0-for-3 today.
3. **Oldest analyses are template slop** (content quality — never auto-fixed): boilerplate filler, mid-sentence truncation, garbled CVSS duplication, mismatched "via Mastodon" attribution on single-source bot CVE notices.
4. **3,649 of 10,254 story titles are truncated at exactly 140 chars**, mid-sentence ("…The group claim — Tia N. List" in the case-page `<title>`). Roughly a third of the site's titles end in a fragment.
5. **`automation_runs` check is vacuous** — `OPENHANDS_AUTOMATION_API_KEY` unset in the audit environment, so all four automations report "no-api-key" and the check passes unconditionally. Today's log reading compensated, but the check is blind.
6. **CTI re-run license defeated by the daily guard** — backlog 49 (>20) can't be drained same-day because cti-pass skips if records were updated today. The 49 will sit until tomorrow.
7. **valtersit.com returns 403** to non-browser clients (referenced from the newest CTI records; also a top-3 ingest source at 262 events/7d) — references won't resolve for programmatic readers.

## Recommendations

1. Add ingest host skips for `euvd.enisa.europa.eu` (or an RSS de-dup against NVD/kevrichment — EUVD advisories are already ingested elsewhere), `dev.to`, and review `radar.offseq.com` for a throttle; handle-skip `euvd_bot`, `ivycyber@privacysafe.social`, `security_crawler_carl@infosec.exchange`, `hackaday@www.urbanmind.net`, `bobe_bot@mastobot.ping.moi`.
2. Codify repeat-coverage demotion into `score.py`: a story already carried by the previous digest with no new event gets demoted manually every day — this is the single most repeated override class.
3. Codify the non-CVE TTP/regulatory uplift into `score.py` (recurring wildcard justification) so the digest agent stops paying wildcard budget for it.
4. Fix title truncation: truncate at word boundary (or drop the fixed 140-char cut) so 36% of titles don't render as mid-sentence fragments in page titles.
5. Suppress or raise the analysis gate for single-source bot-CVE stories — they are what fills the oldest-analyses slop class (samples 2 and 3 today were exactly this).
6. Set `OPENHANDS_AUTOMATION_API_KEY` in the audit environment so `automation_runs` does real work; investigate why ~01:28 UTC automation-service starts abort between PHASE1 and PHASE2 (issue 2).
7. Add an override flag to cti-pass's "already updated today" guard so the auditor's backlog re-run license can actually drain a >20 queue.
8. Refresh `attack/techniques.json` to current ATT&CK — it carries pre-v8 taxonomy (e.g. T1099/"Timestomp" mapped to a non-existent "stealth" tactic; current ATT&CK is T1070.006 under defense-evasion). Records are internally consistent today, but any cross-tool consumer will disagree.

# Tia N. List audit — 2026-08-16

Verdict: **ISSUES** — content quality holds (digest, analyses, CTI all good; site live and current), but the digest automation run FAILED (late dispatch → 30-min timeout kill, output committed 2 min before the kill and is complete) and the hourly engine skipped the 08:00–11:00 UTC slots plus a 07:01 FAILED run. The TONTOU Spectre v2 duplicate (reported 08-14 and 08-15) was fixed this run at the operator's request.

## Deterministic checks

| check | result | detail |
|---|---|---|
| digest_published | OK | exists, 3076B |
| hourly_fresh | OK | newest event 2026-08-16T09:31:14 (4h old; threshold 26h — coarse) |
| last_commit | OK | 2026-08-16T13:05 (0h old) |
| analysis_queue | OK | 2 pending |
| cti_queue | OK | 1 uncovered case |
| ioc_candidates | OK | 156 new candidates |
| store_invariants | OK | 0 bad event refs, 0 cti story_id mismatches |
| sigma_valid | OK | 0 failing rules |
| yara_valid | OK | 0 failing rules |
| automation_runs | **FAIL** | hourly: COMPLETED 13:00 (07:01 run FAILED; slots 08:00–11:00 never dispatched — service fired digest+cti+hourly together at 12:09:57); digest: FAILED 12:09 (late dispatch, killed at timeout); cti: COMPLETED 12:09; safety_net: COMPLETED 2026-08-15T13:30 |
| live_site | OK | 6/6 pages 200 |
| snapshot_pin | OK | 2026-08-16 sha256 match |
| coverage | OK | 72 records, 57 techniques, 11 sigma, 4 yara |
| fragmentation | OK | no duplicate-story fragments (check scoped to Patch-Tuesday roundups — see Issues) |

## Quality sampling

**Digest** (`digests/2026-08-16.md` + `.json`, 3KB): correct coverage-delta structure — emoji headline + exec summary (JSON), themed prose, watchlist close. Covers 6 stories, all genuinely new or evolved (massive-azure-exfiltration dev TODAY with a real new event, Wake NC election vendor dev TODAY, Evooo1Bot, CRPx0 leak site, ICO/ACRO reprimand, Sogang 180k breach); the 9 evolved-but-omitted [UPDATE] candidates (Download More RAM, Entra passkeys, Ruby Marshal chain, TheGentlemen, Storm, PAX Q80, agentic-AI, Carhartt, Zebra) carry only Aug-15 re-report events of substance already covered in yesterday's digest, so the skips are per-spec, not gaps. Specific throughout (Article 32(1)(b)/(d) UK GDPR, 180,000 accounts, SOCKS5/SSH-bruteforce botnet detail), no AI-slop, no em dashes, all 6 story slugs resolve, live page at /daily/2026-08-16/ matches the source byte-for-byte in headline.

**Analyses** (3 newest by mtime: Metasploit weekly wrap-up, Download More RAM, Ruby Marshal.load chain): grounded in the cited research, specific (13 new modules incl. Pterodactyl CVE-2025-49132 CVSS 10.0 and Fragnesia CVE-2026-46300; SPD EEPROM write + DDR4/DDR5 aliasing mechanics with the local-admin privilege caveat; Gem::SpecFetcher autoload chain on Ruby 4.0.6 back to 3.3 with no CVE). No filler, no em dashes, each closes with concrete guidance. Quality good.

**CTI records** (2 newest by updated_at: malicious-SVG DCRat, Entra ID passkeys): schema consistent with the store; ATT&CK codes plausible for DCRat (T1566 phishing / T1027 obfuscation / T1574 hijack / T1055 injection) and correctly empty for the policy story; `detections` flags match reality (sigma for DCRat, none for passkeys); all 3 spot-checked references resolve 200 (gbhackers, cyberpress, microsoft.com). Good.

**Live site**: matrix/index renders hot stories; story pages 200 (incl. the 6 digest stories); CTI page renders the coverage matrix; digest page live with today's content. `/iocs/` 404 is expected (no iocs page in the SSG output spec). TONTOU duplicate pages both render 200 — see Issues.

## Fixed this run

1. **TONTOU Spectre v2 duplicate merged (operator-authorized; outside the strict fix license — fragmentation check is Patch-Tuesday scoped).** Wrote `triage/decisions-2026-08-16-audit.json` (merges only) and ran `triage.py apply`: `tontou-new-cpu-attack-bypasses-spectre-v2-mitigations-leaks-linu` (bleepingcomputer, 2026-08-07) merged into canonical `tontou-cpu-spectre-v2` (malware.news, 2026-08-10). Canonical now holds both events (n_sources 2); fragment is a `merged_into` redirect shell; `new-tontou-…` remains a redirect to the canonical. `ssg.py` validates clean (feeds regenerated, done). Same research confirmed before merging: identical titles modulo colon, same TONTOU Spectre v2 mitigation-bypass story.

No other fix-license item triggered: digest/hourly/CTI outputs all exist (no re-dispatch warranted — the digest is complete; the 12:09 catch-up + 13:00 hourly covered the 08–11 gap); queues small (cti 1, analysis 2); sigma/YARA all valid (nothing to quarantine); git clean and in sync with origin, site current, snapshot pin matches (no ssg.py rebuild needed).

## Issues found

1. **Digest automation run FAILED (timeout kill after late dispatch).** Run ad51fc59 dispatched 12:09:57 (cron is 11:15), committed the complete digest at 12:37, killed at the 30-min timeout 12:39:57 before a clean exit → status FAILED with no error/conversation_id. Output is committed, pushed, and live; the digest itself is complete and correct, so this is a status/reliability issue, not a content loss.
2. **Hourly engine skipped 08:00–11:00 UTC and the 07:01 run FAILED.** Runs API shows no hourly runs between 07:01 (FAILED) and the 12:09:57 catch-up; git log confirms no hourly commits 07:xx–12:17. Data caught up (newest event 09:31 ingested by the 12:09 run, 13:00 run fine), so no data gap — but 4–5 scheduled cycles were silently missed.

## Recommendations

1. Investigate automation-service dispatch reliability: the 11:15 digest, 12:00 cti, and 12:00 hourly slots all fired at 12:09:57 today and the 08–11 hourly slots never fired. The digest run is now routinely squeezed against its 30-min timeout (late dispatch + kill) — if dispatch keeps slipping, consider widening the digest timeout or moving it earlier.
2. Widen the fragmentation detector (or add a title-similarity pass) so same-title/same-research duplicates beyond Patch-Tuesday roundups are caught by the deterministic check and become fix-license-eligible. The TONTOU merge this run required an explicit operator override because the check never flags this class.

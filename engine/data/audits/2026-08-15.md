# Tia N. List audit — 2026-08-15

Verdict: **ISSUES** — all 14 deterministic checks pass, digest/analyses/CTI quality is strong, and all automation runs completed, but the TONTOU Spectre v2 duplicate story reported on 2026-08-14 is still live on the site (two non-redirect case pages for the same research).

## Deterministic checks

| check | result | detail |
|---|---|---|
| digest_published | OK | exists, 9016B |
| hourly_fresh | OK | newest event 2026-08-15T10:06:22 (3h old) |
| last_commit | OK | 2026-08-15T13:11 (0h old) |
| analysis_queue | OK | 2 pending |
| cti_queue | OK | 1 uncovered case |
| ioc_candidates | OK | 157 new candidates |
| store_invariants | OK | 0 bad event refs, 0 cti story_id mismatches |
| sigma_valid | OK | 0 failing rules |
| yara_valid | OK | 0 failing rules |
| automation_runs | OK | hourly COMPLETED 13:00; digest COMPLETED 11:15; cti COMPLETED 12:00; safety_net COMPLETED 2026-08-14T13:30 |
| live_site | OK | 6/6 pages 200 |
| snapshot_pin | OK | 2026-08-15 sha256 match |
| coverage | OK | 72 records, 57 techniques, 11 sigma, 4 yara |
| fragmentation | OK | no duplicate-story fragments (check scoped to Patch-Tuesday roundups — see Issues) |

## Quality sampling

**Digest** (`digests/2026-08-15.md`, 9KB, plus JSON headline): correct structure — headline + exec summary + themed prose + watchlist. Lead story is the Download More RAM SPD/DRAM-aliasing attack with CVE-2026-23670; the rest is organized by theme (identity/auth, ransomware/leak-site, agentic AI, breaches, advisories). Specific throughout: CVEs (CVE-2026-23670, CVE-2026-65400, CVE-2026-58231, CVE-2026-19910/911, HCSEC-2026-28, CVE-2026-12569), quantified claims (89GB Shell, 153GB/433,909 files LiteLLM, $3.3M Carhartt, 8TB Zebra, 600k France), actor names (ShinyHunters, Clop, Storm, TheGentlemen, DeadLock). No AI-slop, no em dashes, all 25 story slugs resolve. Reads like a report a human would trust.

**Analyses** (3 newest by mtime: Download More RAM, Ruby Marshal.load gadget chain, Entra ID passkeys): grounded in the cited research, specific (SPD EEPROM write + DDR4/DDR5 aliasing mechanics; Gem::SpecFetcher autoload chain working on Ruby 4.0.6 back to 3.3, no CVE; passkey default Sep 1 2026 and SMS/voice retirement Feb 1 2027), no filler, no em dashes. Each closes with concrete guidance (watch for SPD write-protection firmware; audit Marshal.load on untrusted data; audit passkey registration before silent factor removal). Quality is good.

**CTI records** (2 newest by updated_at: malicious-SVG DCRat, Entra ID passkeys): schema consistent with the store; ATT&CK codes plausible for DCRat (T1566 phishing, T1027 obfuscation, T1574 hijack execution flow, T1055 process injection); references resolve (all 5 spot-checked returned 200). `detections` flags match reality (sigma for DCRat, none for the Entra ID policy story). No CVEs/actors where none belong. Good.

**Live site**: matrix/index renders hot stories; case page (Download More RAM) shows analysis; CTI page renders the ATT&CK coverage matrix; daily digest page (2026-08-15) shows headline + exec summary + full prose matching the source. All checked pages 200 except the TONTOU issue below.

## Fixed this run

None. No deterministic check failed; queues are small (cti 1, analysis 2); sigma/YARA all valid; git clean and synced with origin; fragmentation check flagged no group; site output current.

## Issues found

1. **TONTOU Spectre v2 duplicate still live (carried over from 2026-08-14).** Two non-redirect case pages exist for the same research: `tontou-new-cpu-attack-bypasses-spectre-v2-mitigations-leaks-linu` (bleepingcomputer, 1 event) and canonical `tontou-cpu-spectre-v2` (malware.news, 1 event; absorbed `new-tontou-...` via `merged_into`). Both pages render 200 with full content on the live site; the merged fragment correctly redirects. Identical titles modulo a colon. The deterministic fragmentation check remains scoped to Patch-Tuesday roundups and did not flag it, so the fix license (clause 5) does not authorize the merge — report only. Unresolved from yesterday; recommend fixing via triage decisions file.

## Recommendations

1. Merge `tontou-new-cpu-attack-bypasses-spectre-v2-mitigations-leaks-linu` into canonical `tontou-cpu-spectre-v2` via a triage decisions file (canonical keeps its URL; fragment becomes a redirect). Unambiguous: identical titles, same research, both events confirmed. Same merge recommended on 2026-08-14; it has not happened.
2. Widen the fragmentation detector (or hourly title-similarity pass) to catch same-title/same-research duplicate stories beyond Patch-Tuesday roundups, so this class of duplicate is caught by the deterministic check and becomes fix-license-eligible instead of requiring manual triage.

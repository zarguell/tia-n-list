# Tia N. List audit — 2026-08-14

Verdict: **ISSUES** — all 14 deterministic checks pass and digest/analyses/CTI quality is strong, but one duplicate story (TONTOU) is live on the site and the deterministic fragmentation check is too narrow to catch it.

## Deterministic checks

| check | result | detail |
|---|---|---|
| digest_published | OK | exists, 16771B |
| hourly_fresh | OK | newest event 2026-08-14T12:10:40 (1h old) |
| last_commit | OK | 2026-08-14T13:19 (0h old) |
| analysis_queue | OK | 2 pending |
| cti_queue | OK | 1 uncovered case |
| ioc_candidates | OK | 144 new candidates |
| store_invariants | OK | 0 bad event refs, 0 cti story_id mismatches |
| sigma_valid | OK | 0 failing rules |
| yara_valid | OK | 0 failing rules |
| automation_runs | OK | hourly COMPLETED 13:00; digest COMPLETED 12:25; cti COMPLETED 12:00; safety_net COMPLETED 2026-08-13T13:30 |
| live_site | OK | 6/6 pages 200 |
| snapshot_pin | OK | 2026-08-14 sha256 match |
| coverage | OK | 60 records, 56 techniques, 10 sigma, 4 yara |
| fragmentation | OK | no duplicate-story fragments (check is scoped to Patch-Tuesday roundups — see Issues) |

## Quality sampling

**Digest** (`digests/2026-08-14.md`, 16.7KB, plus JSON headline/summary): correct structure — headline + exec summary + prose. Prose is organized by theme (breaches, Patch Tuesday + follow-ons, IoT, ransomware/leak-site, APT, supply chain, advisories) and closes with a watchlist. Specific throughout: CVE numbers (CVE-2026-68820, CVE-2026-26035, CVE-2026-70465/468, CVE-2026-67261), counts (1.6M accounts, 296k Dysphoria devices, 548 building-automation devices, 434k pipelines), actor names (ShinyHunters, Anubis, Clop, Jewelbug/Earth Alux, APT36, TeamPCP, Lazarus). No AI-slop, no em dashes. All 44 story slugs resolve to story files. Reads like a report a human would trust.

**Analyses** (3 newest: Dysphoria, DCRat/SVG, RingCentral): grounded in the cited articles, specific (ENS/SNS C2 with fake-IPv6-encoded addresses; HTML smuggling via double-Base64 JS + 7z Blob + Brotli-named DLL sideloading; 623GB theft / 1.6M accounts / July 28 disclosure), no filler, no em dashes. Each closes with concrete detection or action guidance (hunt blockchain DNS queries, alert browser-initiated 7z creation, treat breach-phishing as likely). Quality is good.

**CTI records** (2 newest by updated_at: Windows AFD.sys/Lazarus, APT36 HACKERAI): schema consistent with the rest of the store; ATT&CK codes plausible (T1566, T1204, T1068, T1014, T1562; T1071, T1505); CVE and actor fields populated correctly (CVE-2025-49113 + CVE-2026-68820 for the AFD.sys case; no CVEs for HACKERAI, correct). Spot-checked references: gbhackers 200, cyberpress 200; securityweek and bleepingcomputer returned 403 to a plain curl (bot-blocking, not dead links — both are known-bot-protected domains). 0 story_id mismatches per store_invariants.

**Live site**: index (matrix) renders top stories with scores; RingCentral case page shows score breakdown, both sources, and analysis text; CTI page renders the ATT&CK coverage matrix with amber/green gap semantics; daily page shows today's headline + summary. All 6 checked pages 200. Nothing stale or broken.

## Fixed this run

None. No deterministic check failed; queues are small (cti 1, analysis 2); sigma/YARA all valid; git clean and synced with origin; fragmentation check did not flag a group; site output current (last commit 0h old).

## Issues found

1. **Duplicate live story: TONTOU Spectre v2 CPU attack.** Two live stories with identical titles exist: `tontou-new-cpu-attack-bypasses-spectre-v2-mitigations-leaks-linu` (event `e1368c3668c`, bleepingcomputer, 2026-08-07, appeared in the 08-07 digest) and `tontou-cpu-spectre-v2` (event `mf:159582`, malware.news republish, 2026-08-10, which already absorbed fragment `new-tontou-...` via `merged_into`). Same story, two cards. The triage machinery merged the empty fragment but missed the bleepingcomputer-based story. The deterministic fragmentation check is deliberately scoped to Patch-Tuesday-style roundups (audit.py comment: "the LLM quality sample catches anything subtler") — it did not flag this group. Because the fix license (clause 5) only covers groups the fragmentation check flags, this was reported rather than auto-merged.

## Recommendations

1. Merge `tontou-new-cpu-attack-bypasses-spectre-v2-mitigations-leaks-linu` into canonical `tontou-cpu-spectre-v2` via a triage decisions file (canonical keeps its URL; fragment becomes a redirect). The merge is unambiguous: identical titles, same research, both events confirmed.
2. Consider widening the fragmentation detector (or the hourly triage title-similarity pass) to catch same-title/same-research stories beyond Patch-Tuesday roundups — e.g. identical normalized titles within N days should be merge candidates, not just roundup families.

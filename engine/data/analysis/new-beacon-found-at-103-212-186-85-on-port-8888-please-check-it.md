BeaconBeagle, a public C2 beacon detection service, has flagged multiple new command-and-control infrastructure IPs over the past few days. Beacons detected include 47.94.56.71 on port 8443 (x64 and x86 variants), 103.212.186.85 on port 8888 (x64), and 38.76.183.197 on port 443 (x64). Each detection is published as a public Mastodon post with a link to the beacon's analysis page on beaconbeagle.com.

These are automated threat intel findings from a single source (BeaconBeagle). The IPs have not been attributed to a specific threat actor or campaign in the available reporting. BeaconBeagle identifies implant beacons by matching network behavior against known C2 patterns, but the underlying malware families and targeting context are not specified. The use of standard HTTPS ports (443, 8443) alongside a non-standard port (8888) suggests varied infrastructure setup.

Without additional context from threat intel vendors or incident reports, these beacons are indicators rather than actionable intelligence. Analysts who encounter these IPs in logs should investigate for compromise but should not draw attribution conclusions from BeaconBeagle data alone. The story is worth monitoring if additional reporting ties these IPs to a known campaign or malware family.

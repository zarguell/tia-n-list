The BeaconBeagle threat intelligence feed continues to identify command-and-control (C2) infrastructure across multiple IP addresses and architectures. Recent detections include beacons at 120.53.19.157 on port 443 (both x64 and x86 variants) and 85.175.101.203 on port 80 (both x64 and x86 variants). The original beacon at 103.212.186.85 on port 8888 remains active, with the feed now tracking 15 total beacon events across this aggregation.

The pattern of dual-architecture beacons (x64 and x86) on the same IP addresses suggests a threat actor deploying C2 infrastructure that can target both modern and legacy systems. Port 443 (HTTPS) and port 80 (HTTP) are commonly used to blend C2 traffic with legitimate web traffic, making detection harder. The mix of ports across different IPs indicates either multiple campaigns or a single actor rotating infrastructure.

Organizations should check their logs for outbound connections to these IPs: 120.53.19.157:443, 85.175.101.203:80, and the original 103.212.186.85:8888. The BeaconBeagle feed provides detailed IOCs at beaconbeagle.com for each detected beacon, including architecture-specific indicators. These types of automated beacon detections are useful for threat hunting but should be correlated with other telemetry before taking containment action, as the confidence level varies per detection.

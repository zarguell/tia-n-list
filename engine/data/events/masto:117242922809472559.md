EDR flags a dropped exe, sandbox queue is backed up. A thin PE import table (mostly LoadLibraryA + GetProcAddress) isn't a boring binary, it

via Mastodon https://resources.codelivly.com/product/practical-malware-analysis-guide/

DeadLock, an emerging financially motivated ransomware operation that couples conventional intrusion tradecraft with decentralized infrastructure engineered to survive disruption. First observed in July 2025, the operation uses double extortion: encrypting enterprise data while threatening publication of stolen material. The encryptor’s pre-encryption routine is built to degrade both prevention and recovery. After XOR-decoding an embedded configuration, DeadLock checks default and UI language identifiers and deletes itself when it detects Russian, Ukrainian, Belarusian, several CIS languages, Iran, Syria, Oman or Yemen. This geofencing is not attribution, but is consistent with a regional avoidance pattern. On systems without elevation, the Rust-based malware generates a randomly named CMD file and repeatedly invokes ShellExecuteW with RunAs, attempting to force a UAC approval. Elevated execution unlocks SeDebugPrivilege, SeBackupPrivilege, SeRestorePrivilege, SeTakeOwnershipPrivilege and related privileges. DeadLock then empties Recycle Bins, registers a custom icon for its .dlock extension, and stops processes and services likely to impede encryption. Windows Defender’s windefend service, VSS, Windows Backup, Hyper-V components and Active Directory services are among the listed targets. The process kill list includes MsMpEng, SmartScreen, cloud-sync clients, backup software, remote-access utilities, PowerShell, Task Manager and Explorer. The intent is dual-purpose: release

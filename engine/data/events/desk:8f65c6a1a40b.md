The CVE record reached NVD three months after the April code fixes, describing a JWT authentication mechanism that accepts tokens signed with algorithms other than those explicitly configured and validates them incorrectly, enabling administrative compromise and account takeover. It carries per-product affected ranges including the org.wso2.carbon.apimgt.rest.api.util Maven artifact and notes the 9.8 adjustment for single-tenant deployments. CISA's SSVC assessment now on the record marks exploitation as active and the vulnerability automatable with total technical impact.

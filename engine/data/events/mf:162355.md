One campaign, called “Steamtv Esp,” potentially reached around 570,000 Meta users between June 11 and July 3, 2026, with Spain appearing to be the main target. The campaign uses TV-streaming lures similar to earlier IPTV-themed malware campaigns. Victims who click an ad are taken to a phishing website that checks whether they are using Android. Non-Android visitors cannot access the download button. Android users are guided through steps to allow app installation from unknown sources and enable Accessibility Services. The malicious APK , disguised as a streaming application such as “StrεαmTV Pro,” is then downloaded and installed. ThreatFabric said the payload was hosted on a GitHub repository linked to an operator that previously distributed the Mirax Android trojan. The actor has also used GodFather malware, suggesting continued activity in Android financial fraud operations. StreamRAT Seizes Android Devices StreamRAT is deployed through a dropper that uses HTML pages and embedded JavaScript to guide the victim through installation. The dropper can ask to become the device’s default launcher, allowing it to regain control whenever the user presses the Home button. StreamRAT Seizes Android Devices (Source: threatfabric) A notable feature is its use of a non-functional VPN connection . Before downloading the final payload, the dropper requests VPN permission and routes most device traffic through a connection that does not forward data. The malware itself is excluded from the

NVD record for the WPBookit booking plugin for WordPress, vulnerable up to and including 1.0.6 through missing file type validation in image_upload_handle(), hooked via the add_new_customer route. Unauthenticated attackers can upload arbitrary files, enabling remote code execution.

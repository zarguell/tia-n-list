The attacker didn't push code directly. They got into MemTensor's GitHub Actions release workflow, intercepted the publish tokens before the real job ran, then used those tokens to ship the malicious packages themselves. submitted by /u/BattleRemote3157 [link] [comments]

via reddit r/cybersecurity: MemTensor npm and PyPI packages were backdoored today through their own CI pipeline

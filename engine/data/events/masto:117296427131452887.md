CTF heap challenge segfaults inside libc, not your code. The tell: free() doesn't erase your pointer, it relists the chunk (tcache/fastbin).

via Mastodon https://resources.codelivly.com/product/exploit-development-fundamentals/

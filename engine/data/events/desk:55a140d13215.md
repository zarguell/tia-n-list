NVD record for the Advanced Custom Fields: Extended plugin, vulnerable in versions 0.9.0.5 through 0.9.1.1 via prepare_form() passing user input to call_user_func_array(). Unauthenticated attackers can execute arbitrary code.

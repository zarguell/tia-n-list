{
 "id": "mf:159842",
 "title": "CNCMachineRMS Hides Windows APIs With Runtime Hashing and Ships With No Import Table",
 "kind": "original",
 "source": "cyberpress.org",
 "url": "https://cyberpress.org/cncmachinerms-hides-apis-hashing/",
 "published_at": "2026-08-12T06:22:35Z",
 "cves": [],
 "lang": "en",
 "content_md": "Security researchers have uncovered CNCMachineRMS, a 1.14 MB x64 remote access trojan (RAT) delivered at the end of a BabaDeda-based infection chain. The malware avoids common static-analysis signals by shipping without an import table, resolving Windows APIs from hashes at runtime, and creating its strings only when needed. The attack begins with a ClickFix lure and eventually launches a legitimate, signed IBM SPSS WinWrap Basic IDE executable, WinWrapIDE.exe . Although the signed program is clean, attackers abuse its COM scripting capability to load a malicious DLL . Four decoy DLLs then load through normal Windows import resolution, helping the chain avoid suspicious LoadLibrary activity. The final decoy reads shellcode from model.dat , places it inside a large buffer padded with random data, marks the region executable, and triggers it through EnumTimeFormatsEx . This benign Windows date-formatting API calls the supplied callback, allowing the shellcode to run without a direct malicious function call in the attacker\u2019s code. CNCMachineRMS Hides APIs Hashing That shellcode is a BabaDeda stage. On its own, it may appear harmless in a sandbox: it displays a \u201cNot Found Storage\u201d message and waits because it requires an external configuration file. The required file, HelperStandardizationApplication.bin , has high entropy and resembles encrypted data, but researchers found it is actually an obfuscated serialized property tree containing a script and the embedded CNCMachineRMS pa\n"
}
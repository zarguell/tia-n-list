Todd C. Miller committed the replacement fix on August 28, 2026, stating the earlier change was not effective because mktime() re-reads the TZ environment variable each time it is called. The commit, reported by Ermenson Junior, changes src/sudo.c (+24/-7) to unsetenv("TZ") and make environ distinct from the plugin envp. No released sudo version included it at disclosure.

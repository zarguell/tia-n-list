Qualys analyzed the Apache HTTP Server 2.4.49 path traversal and RCE flaw disclosed October 4, 2021. The vulnerability allows arbitrary file read and remote command execution when path normalization is bypassed and the server runs a non-default directory directive (Require all granted) with mod_cgi enabled. Fix is upgrade to 2.4.51 or greater.

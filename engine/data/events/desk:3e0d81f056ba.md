Bishop Fox (Jon Williams) validated the full unauthenticated chain end to end against UniFi OS Server 5.0.6: an Nginx raw-versus-normalized URI mismatch riding the /api/auth/validate-sso/ exemption reaches the package-update command injection, and the ucs-update account's passwordless sudo over dpkg, chmod, systemctl, and uos escalates one request to a root shell. The post confirms forged HS256 owner tokens built from the signing key at /data/unifi-core/config/jwt.yaml still authenticate against fully patched 5.0.8 consoles, and publishes a safe detection tool, CVE-2026-34908-check, on GitHub.

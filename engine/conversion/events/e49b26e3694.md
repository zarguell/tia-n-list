Mustang Panda is using a multi-stage LNK–PowerShell loader disguised as a fake "Browser Updater" to sideload **PlugX** through a legitimate G DATA antivirus binary (`Avk.exe`). The infection chain: LNK → hidden PowerShell → archive extraction → fake update UI → HTTP download of MSI → DLL sideloading → PlugX C2.

**Tradecraft highlights:** DJB2-style API hashing, `RtlRegisterWait` thread-pool callback injection to obscure RWX execution, RC4+XOR layered encryption of C2 configuration, and per-victim registry-based client ID persisted in `HKCU\Software\Classes` subkeys. C2 observed: `fruitbrat[.]com` over HTTPS with Edge/Chrome User-Agent strings. Targets include government and diplomatic entities consistent with Mustang Panda's historical targeting.

---
